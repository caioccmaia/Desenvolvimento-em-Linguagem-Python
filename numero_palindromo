class Solution:
    def isPalindrome(self, x: int) -> bool:
        l1 = str(x)
        if x < 0:
            return False
        for i in range(len(l1)):
            a = int("".join(l1[::-1]))
            if x == a:
                return True
            else:
                return False
